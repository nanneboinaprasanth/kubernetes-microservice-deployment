apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "portfolio-website.fullname" . }}-config
  labels:
    {{- include "portfolio-website.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

