apiVersion: v1
kind: Secret
metadata:
  name: {{ include "portfolio-website.fullname" . }}-secret
  labels:
    {{- include "portfolio-website.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secret }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

